{{{content}}}
<div class="note-gutter">
	{{#if empty}}
		<p class="empty">(empty note)</p>
	{{else}}
		<div class="content text">
			{{#if note.title}}
				<h1>{{{note.title}}}</h1>
			{{/if}}
			{{#if note.text}}
				{{{markdown note.text}}}
			{{/if}}
		</div>
	{{/if}}
</div>

