<input type="text" name="title" value="{{note.title}}" placeholder="Title">
{{#if show_url}}
	<input type="text" name="url" value="{{note.url}}" placeholder="URL">
{{/if}}
<textarea name="text" rows="1" cols="80" placeholder="Note text">{{note.text}}</textarea>

