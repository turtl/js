{{#if empty}}
	{{#unless child}}
		<p class="page-empty">
			{{icon 'boards'}}
			{{t 'No boards here!'}}<br>
			<small>{{t 'Tap <em>+</em> to start'}}</small>
		</p>
	{{/unless}}
{{else}}
	<ul class="item-list"></ul>
{{/if}}

