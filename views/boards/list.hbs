{{#if empty}}
	{{#unless child}}
		<p class="page-empty">
			{{icon 'boards'}}
			No boards here!<br>
			<small>Tap <em>+</em> to start</small>
		</p>
	{{/unless}}
{{else}}
	<ul class="item-list"></ul>
{{/if}}

