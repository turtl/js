{{#equal actions.length 1}}
	<a href="#action" class="abutton" title="{{actions.0.title}}" rel="main">
		<span>+</span>
	</a>
{{else}}
	<a href="#action" class="abutton" rel="open">
		<span>+</span>
	</a>
	<ul>
		{{#each actions}}
			<li rel="{{name}}" title="{{title}}" class="item-{{@index}}">
				<h4>{{title}}</h4>
				<a href="#{{name}}" class="abutton"><icon>{{{icon}}}</icon></a>
			</li>
		{{/each}}
	</ul>
{{/equal}}

